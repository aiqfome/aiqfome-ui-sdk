# Copiar para .env na mesma pasta (ver .gitignore na raiz do SDK).
# Auth: exemplo alinhado a @aiqfome-org/geraldo-ui 0.3+ (createGeraldoMagaluAuth). Na raiz do SDK: npm run build se precisares de dist/ para tipos via node_modules.
VITE_AIQFOME_ACCESS_TOKEN=
VITE_MAGALU_CLIENT_ID=
VITE_POSTMESSAGE_ORIGINS=https://geraldo-restaurantes.aiqfome.com,http://localhost:5180
