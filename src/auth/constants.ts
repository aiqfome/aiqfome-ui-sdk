/** Base do Magalu ID usada na URL de login/autorização (fluxo code). */
export const MAGALU_ID_LOGIN_BASE = 'https://id.magalu.com/login/';

/** Path do callback OAuth hospedado no Geraldo (sempre associado ao app no portal). */
export const GERALDO_OAUTH_CALLBACK_PATH = '/apps-e-integracoes/auth/callback';

/** Host do Geraldo em produção (`.com`). */
export const DEFAULT_GERALDO_BASE_URL = 'https://geraldo-restaurantes.aiqfome.com';
